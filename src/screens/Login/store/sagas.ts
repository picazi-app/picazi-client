import { put, takeEvery } from 'redux-saga/effects';
// import { saveUserSession, success } from '../../../store/action';
// import { UserFieldInfo } from './types';

// function* onLoginSuccess(type: any", data: UserFieldInfo) {
//   yield put(success(type, data));
// }

// export const loginSagas = takeEvery(
//   "LOGIN_SUCCESS", onLoginSuccess
// )